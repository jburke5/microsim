[tool.poetry]
name = "mcm"
version = "0.0.1"
description = "Open-source cardiovascular simulation framework for population health"
authors = []
license = ""

[tool.poetry.dependencies]
python = "^3.7"
numpy = "^1.16"
pandas = "^0.24.2"
statsmodels = "^0.10.0"
scipy = "^1.3"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
